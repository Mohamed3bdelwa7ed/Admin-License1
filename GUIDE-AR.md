# 📘 دليل مشروع Madar License Admin — شرح كامل بالعربي

> هذا الملف يشرح لك كل شيء: ما هو هذا المشروع، لماذا وجوده، كيف يعمل، كيف تشغّله، كيف تستخدمه، وكيف يرتبط مع برنامج Madar POS.

---

## 1) القصة أولاً — ما المشكلة التي يحلها هذا النظام؟

أنت لديك برنامج كاشير اسمه **Madar POS** تعمل به محلات تجارية. تريد بيعه للزبائن (أو اشتراكات)، لكنك تواجه مشاكل:

| المشكلة | الحل في هذا النظام |
|---|---|
| الزبون ينسخ البرنامج ويشغّله على أجهزة أخرى بدون دفع | **ربط الترخيص بجهاز معيّن** (Device Binding) |
| لا تعرف الترخيص انتهى أم لا | **تاريخ انتهاء** و**تحقق دوري** من السيرفر |
| زبون طلب استرداد وأنت تريد إيقافه | **إلغاء الترخيص (Revoke)** عن بعد من لوحة التحكم |
| تريد تحديد عدد الأجهزة لكل زبون | **حد أقصى للأجهزة** (Max Devices) يفرضه السيرفر |
| البرنامج يعمل بدون إنترنت عند الزبون | نظام **أوفلاين مع فترة سماح** + توقيع تشفيري |

**باختصار:** هذا المشروع هو "مكتب التراخيص" الخاص ببرنامجك. أنت (الأدمن) تنشئ التراخيص، تعطي الزبون مفتاحًا، وزبونك يفعّله داخل Madar POS مرة واحدة، والبرنامج يتأكد دوريًا من السيرفر أن الترخيص سليم.

---

## 2) من هو هذا المشروع؟ (المكونات)

المشروع مقسّم إلى **ثلاثة أجزاء منفصلة تمامًا** (وهذا مقصود):

```
┌─────────────────────┐         ┌─────────────────────┐
│   لوحة التحكم        │         │    Madar POS         │
│   (React Frontend)   │         │   (برنامج الزبون)     │
│   للإدارة فقط        │         │                     │
└─────────┬───────────┘         └──────────┬──────────┘
          │ HTTPS (مع JWT)                 │ HTTPS (مع توقيع Ed25519)
          ▼                                ▼
          ┌─────────────────────────────────────┐
          │        License API (Express)         │
          │   العقل المدبّر: كل المنطق هنا        │
          └──────────────────┬──────────────────┘
                             ▼
                   ┌───────────────────┐
                   │  MongoDB Atlas    │
                   │  قاعدة البيانات    │
                   └───────────────────┘
```

### ماذا يعني كل جزء؟

| الجزء | المجلد | وظيفته |
|---|---|---|
| **Frontend** (لوحة التحكم) | `frontend/` | موقع ويب **أنت فقط** تستخدمه: إنشاء تراخيص، إدارة الزبائن والأجهزة، مشاهدة السجل. لا يحتوي أي أسرار. |
| **API** (السيرفر الخلفي) | `api/` | القلب. يستقبل الطلبات من لوحة التحكم ومن Madar POS، ينفّذ كل القواعد (الحدود، الانتهاء، الإلغاء)، يوقّع الردود بمفتاح تشفيري خاص. |
| **Database** | MongoDB Atlas (سحابي مجاني) | تخزين التراخيص، الزبائن، الأجهزة، السجلات. لا يمكن لأحد الوصول إليها إلا عن طريق API. |
| **Madar POS** | (مشروع منفصل — لا نلمسه أبدًا) | برنامج الزبون. يتصل بـ API فقط عبر 4 نقاط (endpoints) موثّقة. |

### ⚠️ قاعدة ذهبية لا تُكسر

1. لوحة التحكم **لا تتصل** بقاعدة البيانات مباشرة — فقط عبر API.
2. Madar POS **لا يتصل** بقاعدة البيانات أبدًا — فقط عبر API.
3. المفتاح الخاص (Private Key) **لا يوضع** في الفرونت ولا في Madar ولا في قاعدة البيانات — فقط في متغيرات بيئة السيرفر.
4. نحن **لا نعدّل** مشروع Madar إطلاقًا — نعطي مطوّره ملف تعاقد واحد فقط.

---

## 3) كيف تعمل الدورة الكاملة؟ (سيناريو واقعي)

### الخطوة 1 — أنشئ ترخيصًا
تفتح لوحة التحكم → Licenses → Create License → تكتب: اسم الزبون، اسم المحل، مدة الترخيص (مثلاً سنة)، عدد الأجهزة (مثلاً 2).

### الخطوة 2 — أعطه المفتاح
النظام **يولّد مفتاحًا عشوائيًا آمنًا** مثل:
```
MDR-7K2P-X9QZ-4MTA-3BRW
```
تظهر لك شاشة "License Created" مع زر نسخ. ترسل المفتاح للزبون (واتساب، إيميل...).

### الخطوة 3 — الزبون يفعّل البرنامج
داخل Madar POS (بعد أن يضيف مطوّره شاشة التفعيل)، يلصق الزبون المفتاح. البرنامج:
1. يحسب **بصمة الجهاز** (Device ID) — رقم فريد مشتق من عتقّيات الجهاز.
2. يرسل للسيرفر: `activate` + المفتاح + بصمة الجهاز.
3. السيرفر يسجّل: "هذا المفتاح مرتبط الآن بهذا الجهاز".

### الخطوة 4 — التحقق الدوري (Validation)
كل 24 ساعة (قابل للتعديل) يرسل Madar طلب `validate`. السيرفر يرد: "الترخيص فعّال، ينتهي بتاريخ كذا". الرد **موقّع تشفيريًا** فلا يمكن تزويره.

### الخطوة 5 — العمل أوفلاين
المحل قد ينقطع عنده الإنترنت؟ لا مشكلة. البرنامج يحتفظ بآخر رد موقّق ويعمل بشكل طبيعي لمدة **فترة سماح** (14 يوم افتراضيًا). بعد انتهائها يطلب اتصالًا للتحقق.

### الخطوة 6 — حالات خاصة
- **انتهت الصلاحية** → السيرفر يرد "منتهي" (موقّعًا) → البرنامج يظهر شاشة تجديد.
- **ألغيت الترخيص** (زبون لم يدفع مثلًا) → تضغط Revoke في اللوحة → التحقق التالي يُخبره أن الترخيص ملغى.
- **جهاز ثالث بنفس الترخيص** وحد الأجهزة 2 → السيرفر يرفض (DEVICE_LIMIT_REACHED).
- **بدّل الزبون جهازًا** → تعطّل الجهاز القديم من اللوحة، أو يفعّل البرنامج القديم "deactivate" → ثم يفعّل الجهاز الجديد.

---

## 4) لماذا MongoDB Atlas؟ ولماذا هذا التصميم؟

| القرار | السبب |
|---|---|
| **MongoDB Atlas (المجاني M0)** | لا يحتاج سيرفر قاعدة بيانات، مجاني للأبد للاستخدام الخفيف، يعمل على replica set (يدعم المعاملات Transactions). |
| **Express + TypeScript** | خفيف، مجاني، سهل النشر على Render مجانًا، ونوع البيانات (Types) يحمينا من الأخطاء. |
| **React + Vite** | لوحة تحكم سريعة، تنشر مجانًا على Cloudflare Pages كملفات ثابتة. |
| **Ed25519 (توقيع تشفيري)** | حتى لو اعترض أحد الشبكة أو عدّل ملف الكاش عند الزبون، لا يستطيع تزوير رد السيرفر لأن التوقيع لن يصح. المفتاح العام يُعطى لـ Madar، والخاص يبقى عند السيرفر. |
| **JWT + Refresh Tokens** | لوحة التحكم محمية بتسجيل دخول؛ كلمة السر مشفّرة bcrypt ولا تُخزّن نصًا أبدًا. |

> **ملاحظة صادقة:** لا يوجد نظام حماية مثالي على ويندوز — من يملك الجهاز يستط نظريًا تعديل برنامج POS نفسه. هذا النظام يجعل كسر الحماية **صعبًا** (توقيع + تحقق دوري) ويمنع النسخ العرضي بين الأجهزة، وهو أقصى ما يمكن عمليًا.

---

## 5) كيف تشغّل المشروع محليًا؟ (خطوة بخطوة)

### متطلبات مسبقة
- Node.js مثبّت (عندك بالفعل)
- ملف `.env` جاهز (موجود عندك بالفعل في `api/.env` وفيه رابط قاعدة Atlas)

### أ) شغّل السيرفر الخلفي (API)

افتح موجه الأوامر (PowerShell) في مجلد المشروع:

```powershell
cd "C:\License Admin\api"
npm install          # أول مرة فقط
npm run dev          # يشغّل السيرفر على http://localhost:4000
```

سترى:
```
[api] MongoDB connected
[api] Admin account ready: admin@madar.example
[api] License API listening on http://localhost:4000
```

### ب) شغّل لوحة التحكم (Frontend)

افتح موجه ثانيًا:

```powershell
cd "C:\License Admin\frontend"
npm install          # أول مرة فقط
npm run dev          # يشغّل اللوحة على http://localhost:5173
```

### ج) افتح المتصفح

اذهب إلى: **http://localhost:5173**

بيانات الدخول (من ملف `.env`):
- البريد: `admin@madar.example`
- كلمة السر: التي وضعتها في `ADMIN_PASSWORD` (لو استخدمت seed: `admin123` — **غيّرها فورًا**)

### د) أوامر مفيدة إضافية

| الأمر | ماذا يفعل |
|---|---|
| `npm test` (داخل `api`) | يشغّل 57 اختبارًا على قاعدة مؤقتة في الذاكرة (لا يلمس بياناتك) |
| `npm run seed` (داخل `api`) | يضيف بيانات تجريبية (زبائن + تراخيص) لو القاعدة فاضية |
| `npx tsx scripts/smoke-live.ts` (داخل `api` والسيرفر شغال) | اختبار شامل على السيرفر الحقيقي |
| `npm run keygen` (داخل `api`) | يولّد مفاتيح توقيع جديدة لو احتجت يومًا |

---

## 6) كيف تستخدم لوحة التحكم؟ (جولة في الصفحات)

| الصفحة | ماذا تفعل فيها |
|---|---|
| **Dashboard** | نظرة سريعة: عدد التراخيص الكلي/الفعّالة/القريبة من الانتهاء/الملغاة + عدد الأجهزة الفعّالة + آخر النشاطات + جدول "تراخيص تنتهي قريبًا" (اتصل بهؤلاء الزبائن للتجديد!) |
| **Licenses** | الجدول الرئيسي: بحث + فلترة بالحالة + ترقيم صفحات. أزرار: **View** (تفاصيل)، **Renew** (تمديد التاريخ)، **Revoke** (إلغاء). زر **Create License** أعلى الصفحة. |
| **License Details** | كل شيء عن ترخيص واحد: المفتاح (مع زر نسخ)، الزبون، التواريخ، حد الأجهزة، جدول الأجهزة المفعّلة (مع زر Deactivate لكل جهاز)، وسجل كامل (منشأ، مُلغى، جُدّد...) |
| **Customers** | كل زبائنك: كم ترخيصًا عنده، كم فعّالًا، كم جهازًا شغّالًا |
| **Devices** | كل الأجهزة المفعّلة في كل المحلات + آخر تحقق لكل جهاز (لو جهاز "متوقف" منذ زمن = الزبون شال البرنامج أو انقطع عنه النت) |
| **Activity** | سجل تدقيق كامل: من فعل ماذا ومتى (حتى أفعال Madar نفسه مثل تفعيل جهاز) |
| **Settings** | بياناتك، تغيير كلمة السر، والإعدادات الافتراضية للتراخيص الجديدة |

### إنشاء ترخيص — مثال عملي
1. Licenses → **Create License**
2. **Customer**: اسم الزبون (عمر حداد)، المحل (محل النور)، إيميل، هاتف
3. **License**: تاريخ البداية (اليوم)، تاريخ النهاية (بعد سنة)، عدد الأجهزة (2)، ملاحظات
4. اضغط **Create License** → تظهر لك شاشة نجاح مع المفتاح → **انسخه وأرسله للزبون**

> الأفعال الخطيرة (إلغاء ترخيص/تعطيل جهاز) تطلب منك تأكيدًا — لا يمكن الضغط بالخطأ.

---

## 7) الربط مع Madar POS — الجزء الأهم ⭐

### الفكرة
Madar POS لا يحتاج منك أي كود من هذا المشروع. يحتاج فقط **4 نقاط اتصال** عبر HTTPS — كلها موجودة وموثّقة وجاهزة.

### ماذا تعطي مطوّر Madar؟
ملفان فقط:

1. **الملف التعاقدي:** `docs/LICENSE_INTEGRATION_CONTRACT.md`
   فيه كل شيء: شكل الطلبات والردود، التوقيع، العمل أوفلاين، أكواد الأخطاء... مكتوب بحيث ينفّذه دون قراءة هذا المشروع إطلاقًا.

2. **المفتاح العام** (يحصل عليه تلقائيًا):
```
GET  https://سيرفرك/api/v1/client/public-key
```
يرد عليه بـ:
```json
{ "keyId": "kid-...", "algorithm": "ed25519", "publicKey": "MCowBQ..." }
```
هذا المفتاح **عام** ولا مشكلة لو انكشف — وظيفته فقط التحقق من صحة الردود.

### النقاط الأربع التي سيرميها Madar

| # | الطلب | متى | المدخل |
|---|---|---|---|
| 1 | `POST /api/v1/client/activate` | أول مرة، عند إدخال الزبون المفتاح | المفتاح + بصمة الجهاز + اسم الجهاز + nonce |
| 2 | `POST /api/v1/client/validate` | كل 24 ساعة تلقائيًا | المفتاح + بصمة الجهاز + nonce |
| 3 | `POST /api/v1/client/deactivate` | عند نقل الترخيص لجهاز جديد | المفتاح + بصمة الجهاز + nonce |
| 4 | `GET /api/v1/client/public-key` | مرة عند التشغيل الأول (أو عند تغيّر المفتاح) | لا شيء |

### مثال: طلب تفعيل حقيقي

ما يرسله Madar:
```json
{
  "licenseKey": "MDR-7K2P-X9QZ-4MTA-3BRW",
  "deviceId": "A1B2C3D4E5F6071829",
  "deviceName": "ALNOOR-CASHIER-01",
  "nonce": "9f2c1a7b..."
}
```

ما يرد به السيرفر (200):
```json
{
  "keyId": "kid-...",
  "algorithm": "ed25519",
  "payload": {
    "type": "activation",
    "licenseKey": "MDR-7K2P-X9QZ-4MTA-3BRW",
    "deviceId": "A1B2C3D4E5F6071829",
    "nonce": "9f2c1a7b...",
    "status": "active",
    "expirationDate": "2027-09-01",
    "maxDevices": 2,
    "validationIntervalHours": 24,
    "gracePeriodDays": 14,
    "serverTime": "...",
    "issuedAt": "..."
  },
  "signature": "base64..."
}
```

### ماذا يجب أن يفعل Madar بالرد؟ (على مطوّره)
1. **يتحقق من التوقيع** بمفتاح Ed25519 العام (خطوات دقيقة داخل ملف التعاقد، ومع أمثلة لـ .NET).
2. **يطابق الـ nonce** الذي أرسله مع الموجود في الرد (يمنع إعادة استخدام ردود قديمة).
3. **يخزّن الرد الموقّع محليًا** (كاش) — منه يعرف التاريخ النهائي وفترة السماح.
4. **يعمل أوفلاين** طالما: `الآن < تاريخ النهاية` و `مضى أقل من (24 ساعة + 14 يوم) على آخر تحقق ناجح`.
5. لو رد السيرفر 403 موقّعًا بـ `LICENSE_EXPIRED` أو `LICENSE_REVOKED` → يعرض شاشة "جدد ترخيصك" / "تواصل مع المزوّد".

### بصمة الجهاز (deviceId) — مهم جدًا
يحسبها Madar من **عتدّيات ثابتة** (UUID اللوحة الأم + الرقم التسلسلي للقرص مثلًا، ثم SHA-256). يجب أن تكون:
- نفسها بعد كل إعادة تشغيل
- مختلفة بين أي جهازين
- أحرف وأرقام فقط، 8–128 خانة

### ملخص فترة السماح (Offline/Grace)
```
آخر تحقق ناجح ⟶ يعمل طبيعيًا (حتى بدون نت)
              ⟶ خلال 24 ساعة: يحاول التحقق بالخلفية عند توفر النت
              ⟶ حتى 14 يومًا بدون أي تحقق: يستمر بالعمل (إنذار للزبون)
              ⟶ أكثر من 14 يومًا: يطلب اتصالًا إجباريًا للتحقق
بعد تاريخ النهاية ⟶ خارج الترخيص (شاشة تجديد)
```

---

## 8) النشر على الإنترنت (مجاني تقريبًا)

| المكوّن | المنصة | التكلفة |
|---|---|---|
| قاعدة البيانات | **MongoDB Atlas** (M0) | مجاني |
| السيرفر API | **Render** (Web Service) | مجاني (ينام بعد خمول — أول طلب بطيء) |
| لوحة التحكم | **Cloudflare Pages** | مجاني |

### خطوات موجزة (التفاصيل في README.md):
1. **Atlas**: أنشئ حسابًا + Cluster + مستخدم + خذ رابط `mongodb+srv://...` (عندك جاهز).
2. **Render**: اربط مستودع Git → Root directory: `api` → Start: `npm run start` → ضع متغيرات البيئة من `api/.env` (نفسها: MONGODB_URI, JWT_SECRET, ADMIN_*, المفاتيح الثلاثة للSigning، و `CORS_ORIGINS=https://اسمك.pages.dev`).
3. **Cloudflare Pages**: اربط المستودع → Build: `npm run build` → Output: `frontend/dist` → متغيرات: `VITE_API_MODE=http` و `VITE_API_BASE_URL=https://سيرفرك.onrender.com`.
4. أضف رابط Cloudflare إلى `CORS_ORIGINS` في Render وأعد النشر.

> ⚠️ لا تضع أي كلمة سر أو رابط Atlas في Git أبدًا — فقط في متغيرات البيئة داخل المنصة.

---

## 9) الأسئلة الشائعة

**س: هل يستطيع الزبون مشاركة الترخيص مع صديقه؟**
ج: صديقه يحاول التفعيل → السيرفر يفحص: هل عدد الأجهزة الفعّالة < الحد الأقصى؟ لا → رفض. ولو فعّل قبل امتلاء العدد، يظهر لك في صفحة Devices جهاز غريب → تعطّله بضغطة زر.

**س: الزبون حذف ملف الكاش أو غيّره؟**
ج: الملف المخزّن موقّع تشفيريًا؛ أي تعديل يجعل التوقيع غير صالح → يعتبره Madar كأنه لا شيء → يطلب تحققًا جديدًا.

**س: غيّر الزبون ساعة جهازه للخلف ليطيل الترخيص؟**
ج: التواريخ المهمة موقّعة من السيرفر (serverTime/expirationDate)، والفترة القصوى بدون اتصال هي فترة السماح (14 يوم) من آخر تواصل حقيقي — فلا يستفيد شيئًا عمليًا.

**س: أريد تغيير سعر/مدة التراخيص الافتراضية؟**
ج: Settings في اللوحة → License Defaults.

**س: نسيت كلمة السر؟**
ج: عدّل `ADMIN_PASSWORD` في `.env` وأعد تشغيل السيرفر — سيحدّث الحساب تلقائيًا.

**س: أين أجد سجل كل ما حدث؟**
ج: صفحة Activity (كل فعل مسجّل: من، ماذا، متى — حتى تفعيلات Madar).

**س: هل يعمل Madar بدون هذا النظام؟**
ج: حتى يضيف مطوّره مكوّن التفعيل، نعم يعمل كالمعتاد — الربط إضافة، والملف التعاقدي هو الجسر.

---

## 10) خريطة الملفات — أين أجد ماذا؟

```
C:\License Admin\
├── README.md                          ← دليل الإنجليزي الكامل (نشر، إعداد، اختبارات)
├── GUIDE-AR.md / هذا الملف             ← الشرح بالعربي
├── PRD.md                             ← المواصفات المطلوبة منك أصلًا
├── UI.md                              ← مواصفات واجهة اللوحة
├── Update_database.md                 ← أمر الترحيل من SQLite إلى MongoDB (تم تنفيذه)
├── docs/
│   ├── API.md                         ← توثيق كل نقاط الـ API بالتفصيل
│   ├── SECURITY.md                    ← نموذج التهديد والحماية
│   └── LICENSE_INTEGRATION_CONTRACT.md ← ⭐ الملف الذي يُعطى لمطوّر Madar
├── frontend/                          ← لوحة التحكم (React)
│   └── .env                           ← VITE_API_MODE + VITE_API_BASE_URL
└── api/                               ← السيرفر (Express)
    ├── .env                           ← كل الأسرار هنا (لا يُرفع لـ Git)
    ├── .env.example                   ← نموذج بأسماء المتغيرات فقط
    ├── scripts/
    │   ├── seed.ts                    ← بيانات تجريبية
    │   ├── keygen.ts                  ← توليد مفاتيح التوقيع
    │   ├── patch-env.ts               ← يعبّئ .env تلقائيًا
    │   ├── smoke-live.ts              ← اختبار شامل على السيرفر الشغّال
    │   └── migrate-sqlite-to-mongodb.ts ← ترحيل قاعدة SQLite القديمة إن وُجدت
    └── tests/                         ← 57 اختبارًا (vitest)
```

---

## 11) خلاصة من 5 أسطر

1. **هذا نظام تراخيص** لبرنامج Madar POS: ينشئ مفاتيح، يربطها بأجهزة، يراقبها، ويوقفها عند الحاجة.
2. **ثلاث طبوات منفصلة**: لوحة تحكم (لك) → API (العقل) → MongoDB Atlas (الذاكرة)، وMadar يتصل بالعقل فقط.
3. **التشغيل**: `npm run dev` داخل `api` ثم داخل `frontend` → افتح localhost:5173 → سجّل دخولك → أنشئ ترخيصًا.
4. **الربط مع Madar**: أعطِ مطوّره `docs/LICENSE_INTEGRATION_CONTRACT.md` فقط — البروتوكول جاهز ويعمل أوفلاين بفترة سماح.
5. **النشر مجاني**: Atlas (قاعدة) + Render (API) + Cloudflare Pages (لوحة) — والأسرار في متغيرات البيئة فقط.
